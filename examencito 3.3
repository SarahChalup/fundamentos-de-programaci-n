// examencito 2.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;
#define max 100

void leer(int vector[], int tam);
bool partidario (int vector[], int tam);

void main()
{
	int tam, vector[max];
	bool r;
	cout << "Ingrese el tamaño del vector"<<endl;
	cin>> tam;
	cout << "Ingrese el vector"<<endl;
	leer(vector,tam);
	if (r==true)
		cout<<"Es partidario";
	else
		cout <<"no se partidario";
	getch();
}

void leer(int vector[], int tam)
{
	for(int i=0; i<tam; i++)
		cin >> vector[i];
}

bool partidario (int vector[], int tam)
{
	bool r=false;
	for (int i=0; i<tam; i=+2)
		for (int j=1; j<tam; j=+2)
			if(vector[i]> vector[j])
				return true;
			
	
		return r;
}
