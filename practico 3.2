
#include "stdafx.h"
#include <iostream>
#include "conio.h"
#include "string"
using namespace std;

void contar(string frase);
#define max 100
void main()
{
	string frase;
	cout<< "meta su frase"<<endl;
	getline(cin,frase);
	contar(frase);
	getch();
}

void contar(string frase)
{
	int tam=frase.length();
	int vocal=0,consonante=0,espacio=0;
	for (int i=0;i<tam; i++)
		{if (frase[i]=='a' || frase[i]=='e' || frase[i]=='i' || frase[i]=='o' || frase[i]=='u' )
			vocal++;
		else {
			if (frase[i]!=' ')
				consonante++;
			else 
				espacio++;
			}
	}
cout << "la frase tiene "<< vocal << " vocales"<<endl;
cout << "la frase tiene "<< consonante << " consonantes"<<endl;
cout << "la frase tiene "<< espacio+1 << " palabras"<<endl;

}


