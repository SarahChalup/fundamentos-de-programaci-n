#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;

int suma (int n);
void main() 
{
	int n,r;
	cout << "Ingrese la cifra";
	cin >> n;
	r=suma( n);
	cout << r;
	getch();
}
 
int suma (int n)
{
	int i=0,s=0;
	while (n!=0)
	{
		s=s+ n%10;
		n=n/10;
	}
	return s;
}
